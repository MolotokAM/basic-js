const CustomError = require("../extensions/custom-error");

module.exports = function countCats(/* matrix */) {
  throw new CustomError('Not implemented');
  module.exports = function countCats(/* matrix */) {
    throw new CustomError('Not implemented');
    for (let i = 0 ; countCats.length; i++) {
      for (let j = i ; i++; j++) {
        if(countCats[i][j] === '^^'){
          number ++;
          i++;
        }
      }
    console.log(number); 
    }
    return number;
  };
};
